# Codex project configuration.
#
# A CLI-exclusive repository declares no MCP servers and no ambient authority:
# the peer on the other side is invoked as a child process with explicit argv
# (see docs/protocol/peer.md), never registered as a server a later session
# inherits without asking. scripts/harness_preflight.py fails closed if either
# an mcp_servers table or an ambient approval/sandbox key reappears here.
personality = "friendly"
