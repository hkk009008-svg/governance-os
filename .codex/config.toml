# Project-scoped Codex app configuration.  The app and CLI share this project
# config in a trusted checkout; Pipeline contributes only the team transport.
# Personal model, personality, sandbox, approval, and credentials stay in user
# configuration.

[mcp_servers.pipeline-team]
command = "./bin/pipeline"
args = ["team", "serve", "--member", "codex"]
