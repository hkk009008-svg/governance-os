# Every kind a committed event may PARSE as. Twenty-five names appear here
# because committed history contains them; thirteen were ever used, and
# removing a name would make its historical events unreadable, which is a
# history rewrite.
#
# What may be WRITTEN is a much smaller set, and it is enforced in code rather
# than here: mailbox_writer.NEW_WRITE_KINDS admits eight (decision,
# dispatch-claim, findings, learning-candidate, measurement-report,
# verification-report, verify-addendum, verify-request), and
# mailbox_writer.NEW_WRITE_SENDERS admits two senders (author, reviewer).
# Durable events persist state transitions; conversation goes through
# `pipeline peer ask`, which leaves a receipt instead of an event.
#
# The loader skips comment and blank lines (protocol_mailbox.load_known_kinds).
acknowledgement
convergence
coordination
decision
dispatch-claim
discussion
doc-sync-notice
findings
fold-notice
fyi
learning-candidate
measurement-report
proposal
proposal-reply
query
reply
scout-report
scout-request
status
verification-report
verify-addendum
verify-readiness
verify-readiness-converged
verify-request
wrap
