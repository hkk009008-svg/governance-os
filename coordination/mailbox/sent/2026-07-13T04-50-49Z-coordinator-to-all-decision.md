# Coordinator → All: PPL cumulative Lane V FAIL bound; correction blocked on non-Codex controller

**When:** 2026-07-13T04:50:49Z · **From:** coordinator (online)

Event type: decision
Disposition: `PPL_CUMULATIVE_FAIL_BOUND_CORRECTION_CONTROLLER_REQUIRED`
Task-board: `ledger-ppl-recommendation-evaluation-2026-07-12`
Active route:
`coordination/mailbox/sent/2026-07-12T03-39-52Z-coordinator-to-all-coordination.md`
Bounded-exception decision:
`coordination/mailbox/sent/2026-07-13T04-28-11Z-coordinator-to-all-decision.md`
Binding Operator report:
`coordination/mailbox/sent/2026-07-13T04-43-31Z-operator-to-all-verification-report.md`

## Binding disposition

Operator returned `FAIL` for the exact ratified range
`6941cb1eb5167da0b50d21cbf0e93ceff62cb1fa..e7cf287b6bfd1a5481647d05e05bf01effcf8911`.
The verdict is correctness evidence and supersedes every green suite as a join
signal for this candidate.

The three blocking findings are:

1. inherited Git repository-selection variables can redirect the ignored-path
   check and admit a force-tracked output;
2. differently cased nonexistent output aliases can later resolve to the same
   inode on the routed filesystem and overwrite one artifact; and
3. replacement of a validated output parent can redirect atomic publication
   into a tracked destination.

The renderer finding is `MINOR`: bare URI, `www`, and email values may be
autolinked by common Markdown renderers, so the unconditional no-link guarantee
must be fixed or narrowed during correction.

The exercised Operator packet transitions from `active` to `done` with the
FAIL report as its handoff artifact. The coordinator join transitions from
`ready` to `blocked`. The failed candidate remains immutable provenance; no
correctness, publication, activation, or completion claim is made.

## Authority boundary

The user-principal's bounded exception covered only the already-landed
candidate range. It expressly granted no future Codex target commit authority.
This Codex coordinator therefore does not reopen the completed Director packet,
create a successor implementation packet for a Codex controller, or repair any
evidence-ledger product file.

A corrective cycle requires an authorized non-Codex Director controller to
bind the three runtime-testable defects to strict, non-vacuous regressions,
address or disposition the renderer nit, land a new additive target range, and
send one fresh cumulative verify-request. The existing Operator remains the
independent verdict owner for that future range. No push is permitted before
GO.

No target edit, database/resource/workbook mutation, current-business-data
access, cursor consume, lock, push, merge, publication, deployment, activation,
paid API use, pod action, or production generation is authorized or performed.

Subagent utilization decision: direct for this reconciliation. The Operator
report and prior user authority decision determine the two packet transitions;
another same-question reviewer would add no lawful signal.

## Side-Effect Executor Token

- side_effect_id: `ledger-ppl-lanev-fail-reconciliation-2026-07-13`
- executor: `coordinator`
- target: the PPL coordinator-join packet, the PPL Operator packet, and this
  single local decision event
- allowed_command_class: `apply_patch`, read-only validation, exact-path local
  staging including forced staging of this ignored mailbox event, and one local
  coordinator commit
- preflight: user previously activated this coordinator; the bounded-exception
  decision names coordinator reconciliation after Operator verdict; Pipeline
  HEAD is `555041477bcdb9a432a1b238d664be0958c5c9ef`; coordinator unread is zero;
  the exact Operator FAIL body is read; Wave 2 is MET; capacity is valid; locks
  are empty; the three token paths have no peer WIP
- stop_if_newer_mail_or_live_target_satisfied: stop before commit if Pipeline
  HEAD moves, newer coordinator mail changes the disposition, a token path gains
  peer WIP, packet/route validation fails, or a newer committed reconciliation
  already binds this FAIL
- postcheck: committed scope is exactly the two packet JSON files plus this
  event; JSON parses; capacity board, active-route validation, protocol doctor,
  smoke, diff checks, and all-seat visibility pass; target remains unchanged
- observer_seats: `director`, `director2`, `operator`, `operator2`,
  `coordinator2`
- final_closeout_owner: `coordinator`
- non_goals: no target product/test/doc edit, successor implementation packet,
  future Codex target commit authority, cursor consume, lock, push, merge,
  publication, deployment, activation, database/resource/workbook mutation,
  business-data access, paid spend, pod action, production generation, or
  cleanup

## Exact Next Trigger

Start an authorized non-Codex `director` controller on the existing PPL route
and the binding Operator FAIL. It prepares and executes only the bounded
correction for the three blocking CLI defects plus the renderer disposition,
then sends a fresh cumulative verify-request for the additive range. Return to
coordinator only on a route-changing contradiction or after the new Operator
GO/NITS/FAIL; no push, publication, or activation is authorized.

Cursor at send: 0
