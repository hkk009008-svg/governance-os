#!/usr/bin/env bash
# Read-only compatibility diagnostics for Cursor Desktop app seats.
set -euo pipefail

ROOT=$(cd "$(dirname "$0")/../.." && pwd)
export PYTHONPATH="$ROOT${PYTHONPATH:+:$PYTHONPATH}"
exec python3 "$ROOT/scripts/cursor_seat_launcher.py" "$@"
