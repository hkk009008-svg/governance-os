# Coordinator Audit: Evidence-Ledger Completion Runway

When: 2026-07-12
Seat: coordinator
Wave: 2
External input:
`/Users/hyungkoookkim/Downloads/evidence-ledger-repo-analysis-and-codex-runway.md`
Disposition: accepted as a non-canonical issue inventory; rejected as the
ordered or executable program route.

## Findings First

### Blocking staleness

The input was reviewed against GitHub `main` at `36f5506` and explicitly did
not include local worktrees, local SDD evidence, or unpushed commits. The live
routed target is the clean linked worktree
`/Users/hyungkoookkim/Pipeline/.worktrees/evidence-ledger-workbook-refresh-2026-07-11`
at `e80ec39`. Since the input baseline, the owner-approved PPL recommendation
pivot landed at `6941cb1`, its approved evaluation plan landed unchanged at
`14a0e53`, and Task 1 authority-contract implementation and hardening landed
through `e80ec39`.

The input therefore cannot replace the active route or current target plan.
Its proposed linear order `R0 -> P2A -> P2B -> F3 -> P3 -> P4 -> P5 -> P6`
also cannot be imposed over the later owner-approved pivot. The current PPL
evaluation foundation is deliberately retrospective, read-only,
non-actionable, and activation-ineligible; it is not permission to ship live
AI, advice, UI, or production mutation.

### Accepted guidance

- Trust before convenience; database formulas and immutable revision/evidence
  boundaries remain authoritative.
- No real business data in git, route artifacts, prompts, fixtures, or review
  output.
- No old-migration edits; client writes remain narrow server-stamped RPCs.
- Implementer and verifier remain separate, with independent adversarial
  review for auth, authority, side-effect, schema-trust, and parseable-output
  surfaces.
- The following are confirmed or credible residual issues for later bounded
  routing: nullable `commission_model` versus non-optional Swift decoding;
  trimmed nonblank supersession reasons; structural two-owner authorization;
  agency future-year parsing; result-history/read-model/iOS entry work; PPL
  correction/versioning; and formula owner rulings.
- PPL interactive forms require an approved identity/revision ADR before
  schema or UI implementation.
- Catalog, dashboard, chat, prediction, learning, external-anchor, deployment,
  and distribution work require fresh plans and owner gates at their actual
  phase boundaries.

### Rejected or deferred guidance

- Do not replay the input's R0 bootstrap against the stale normal checkout.
- Do not fetch, pull, switch, create a new branch, or refresh the normal target
  checkout; the current route binds the linked worktree.
- Do not automatically rewrite target `AGENTS.md` or add a parallel review
  protocol. Pipeline already governs Codex seat authority, and the active plan
  owns its controller/reviewer split.
- Do not dispatch the input's P3-P5 implementation now. Its catalog/chat/
  prediction contracts must be rebased against the approved PPL pivot and the
  interfaces produced by the active evaluation plan.
- Do not treat historical test counts, GitHub baseline claims, recommended
  filenames, provider/model choices, Apple requirements, managed-Supabase
  mechanics, external anchoring, production accounts, push, publication, or
  paid services as current facts or granted authority.
- Do not let a green capacity board, smoke run, or prose plan substitute for
  live Operator GO on an implementation range.

## Rebased Coordination Plan

### Phase A — preserve and finish the active evaluation foundation

This phase remains the only active product route.

- Director owns the sequential approved Tasks 1-9 in the routed linked
  worktree and remains the sole target controller/committer.
- Operator remains blocked until the cumulative verify-request, then returns
  one GO/NITS/FAIL for the exact range.
- Director2 and Operator2 retain only their existing bounded plan/route and
  environment/privacy preflights. They do not implement the downloaded
  runway.
- Coordinator converges only after the live Operator verdict and executable
  evidence. No new packet, lock, push, data access, or production activation
  is authorized here.

Join condition: the active plan's Tasks 1-9, cross-model adversarial reviews,
cumulative verify-request, Operator verdict, privacy/no-write evidence, and
current route/doctor/smoke gates all close.

### Phase B — post-join delta inventory

Activate only after Phase A joins or a durable route-changing contradiction
stops it.

- Director2 prepares a read-only delta inventory mapping the accepted issue
  candidates to current target code, existing plans, ADRs, and owner rulings.
- Operator2 independently verifies only claim presence, dependency order,
  write-set separability, and whether each item is already fixed, still open,
  owner-gated, or obsolete.
- Director and Operator remain available for the active lane's correction or
  closeout loop; they do not duplicate the delta audit.
- Coordinator selects the smallest next executable phase from the verified
  delta. The external input remains evidence, not authority.

### Phase C — bounded trust-gap repairs

Candidate dual-pair split, activated only after Phase B verification:

- Pair A: iOS nullable commission-model decoding, `미정` rendering, planned
  stage labeling, and focused XCTest coverage.
- Pair B: new-migration trimmed nonblank supersession-reason fence with direct
  and RPC-path database tests.

The chunks have disjoint production write sets but require separate linked
worktrees, separate verify-request/report loops, current target instructions,
fresh RED evidence, and truth-doc updates only where claims actually change.

The structural two-owner authorization fence is not part of this parallel
split. It is a separate adversarial campaign requiring an owner-approved ADR,
design-time independent abuse-case enumeration, third-user/anon/spoofed-claim
tests, and cross-model review before implementation.

### Phase D — broadcast system-of-record completion

Execute sequentially at interface boundaries:

1. Pair A implements and verifies the database result-history/read contract.
   Pair B performs bounded contract preflight only.
2. After the DB contract has Operator GO, Pair B implements and verifies the
   iOS API/read model, forms, conflict refresh, audit UI, and pagination in
   independently reviewable chunks. Pair A performs bounded integration
   preflight only.
3. Phase acceptance requires synthetic end-to-end evidence first. Real owner
   use, backup, and any declaration retiring Excel for new broadcast entry are
   explicit owner-gated actions, not coordinator assertions.

### Phase E — PPL identity, formulas, and data lifecycle

- Start with an owner-decision packet and append-only ADR for PPL logical
  identity, revision/supersession, voids, allocation versions, and agency
  reload coexistence.
- Batch the fixed-fee, B.E.P basis, PPL month attribution, reconciliation, and
  result-quantity rulings for the owner. No seat guesses them.
- After approval, route schema/views/RPCs to one pair and independent
  verification to its Operator; route UI only after the DB interface is GO.
- Route the agency base-year contract as a separate parser/test chunk when it
  cannot conflict with import work.

### Phase F — evidence substrate and owner-facing intelligence

The downloaded P3-P5 sections are requirements inventory only. Before any
implementation, rebase them against the completed PPL evaluation interfaces:

1. disposable managed-Supabase executor feasibility and authority ADR;
2. versioned catalog plus evidence-before-return execution;
3. deterministic dashboards and proof drilldown;
4. deterministic Korean numeric/entity/period verifier;
5. bounded grounded-question loop;
6. prediction/grade/reflection/lesson state machine;
7. production hardening, backup/restore, proof export, deployment, and device
   distribution.

Each numbered capability gets its own detailed plan, adversarial classification,
owner/privacy/spend gate where applicable, and separate implementation and
verification loop. No live provider, external anchor, production account,
push, publication, or device action is implied by this plan.

## Capacity And Conflict Rules

- Do not activate Phase B-F packets while Phase A owns the same target and
  interfaces.
- Prefer two independently reviewable deliverables only when their write sets
  are disjoint and their interface dependency is already closed.
- Shared-schema, money, authority, evidence-chain, and UI-on-new-DB-contract
  changes remain sequential.
- One implementation controller and one independent Operator verdict per
  chunk; no redundant unchanged-commit review.
- Coordinator may reconcile packets, gates, handoffs, and owner decisions but
  never repairs evidence-ledger production behavior.

## Subagent Utilization

One bounded read-only helper independently compared the external runway with
the routed worktree and current pivot. It found the same baseline/order
conflict and confirmed the nullable Swift field as a still-open candidate.
The helper did not edit, route, consume, issue GO, access business data, or
take side effects. Coordinator retains this disposition and phase plan.

## Side-Effect Executor Token

- side_effect_id: `ledger-completion-runway-audit-plan-2026-07-12`
- executor: `coordinator`
- target: this single Pipeline handoff artifact
- allowed_command_class: `apply_patch`, read-only validation, exact-path local
  staging, and one local coordinator documentation commit
- preflight: explicit user request to audit and plan as coordinator; coordinator
  unread zero; active route read; target linked worktree clean at `e80ec39`;
  Pipeline smoke and capacity board valid; external input read completely
- stop_if_newer_mail_or_live_target_satisfied: stop before commit if Pipeline
  HEAD moves, new coordinator mail lands, this path gains peer WIP, the target
  worktree becomes dirty, or a newer committed coordinator plan already
  satisfies the request
- postcheck: committed scope is this path only; diff check, Pipeline smoke,
  capacity board, and protocol doctor pass; active route and packets remain
  unchanged
- observer_seats: `director`, `director2`, `operator`, `operator2`,
  `coordinator2`
- final_closeout_owner: `coordinator`
- non_goals: no target product/plan edit, packet mutation, new active route,
  cursor consume, lock, fetch/pull/checkout refresh, push, merge, publication,
  database/workbook/resource access, cleanup, paid spend, pod action,
  production generation, deployment, or device action

## Exact Next Trigger

Continue the active Director on
`director-ledger-ppl-recommendation-evaluation-implementation` from target
HEAD `e80ec39`. Return to coordinator on a route-changing contradiction or
after the cumulative Director verify-request and live Operator verdict. Only
then activate Phase B delta inventory; do not distribute Phase C-F production
packets before that gate.
