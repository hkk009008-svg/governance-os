# Target-Aware Evidence-Ledger Opus Bridge Plan

**Status:** Removed from the program.

## Executable Work

None. Do not enter the evidence-ledger repository, build a target review bridge, extend provider state, create target scopes or routes, request target verification, or write target handoffs.

Cross-model verification for Pipeline work uses ordinary non-author-model Operator staffing. It does not require a cross-repository or provider bridge.

## Exact Stop

Do not replace this plan. Git history preserves the former design.
