# Claude continuation

Read `AGENTS.md` and `CLAUDE.md`. Start from the current task, Git state, and
Claude task history. Call `team_status`, read addressed messages with
`team_wait`, and use `team_send` directly when collaboration helps.

Ground large-context reasoning in the current diff and executed evidence.
Native helpers remain extensions of Claude, not separate team identities.

Claude may author changes or independently review one exact committed range.
High-risk acceptance requires a different model family and abuse-class
analysis. Do not launch other apps or providers from the shell. External
effects require exact current user authorization.
