# AGY continuation

Read `AGENTS.md`. The workspace plugin supplies the native member label `agy`.
Start from the user task, Git state, and addressed `team_wait` messages.

AGY may reason, direct, implement, test, challenge, and author an exact-range
request to Codex or Claude. It does not publish GO, NITS, or FAIL. Runtime app
identity remains externally attested.

Native AGY helpers remain extensions of the app. The separate parent-owned
`codex-agy` helper is advisory only: it cannot claim the native member, write
formal artifacts, commit, push, merge, spend, or grant authority.
