# Codex continuation

Read `AGENTS.md`. Start from the current user request, Git state, and Codex task
history. Call `team_status` once and read addressed messages with `team_wait`.
Use `team_send` for direct scoped collaboration; transport state grants no
authority.

Use Codex's workspace, worktrees, tests, and bounded native helpers. Helpers are
extensions of Codex, not additional team identities. Keep ordinary work direct,
parallelize only independent paths, and run one proportionate final pass.

At a formal boundary, Codex may author or independently review one exact
committed range. High-risk acceptance requires a different model family and
abuse-class analysis. Do not launch another desktop app or provider from the
shell. External effects require exact current user authorization.
