# Four-seat coordination — protocol extension (agent-agnostic pointer)

> The canonical text for this extension lives in the Claude-specific tree:
> [`docs/protocol/claude/four-seat-extension.md`](../claude/four-seat-extension.md).
>
> The four-seat model, lane ownership (Rule #23), the broadcast (`all`) mailbox
> target, and the `coordinator` on-demand policy are not Claude-specific mechanics
> — they apply to any agent seated in this repo. The extension was authored in the
> `claude/` tree before the two-tree convention
> ([migration-map §divergence-note-5](../migration-map-claudemd-split.md)) was
> applied to it; until a full relocation, treat the `claude/` copy as authoritative
> for both trees.
>
> Non-Claude tools reading `AGENTS.md` as their router should follow the pointer
> above for the full seat model, lane ownership (Rule #23), and coordinator policy.

<!-- Two-tree stub per docs/protocol/migration-map-claudemd-split.md (divergence
     note 5). When the extension is next substantively edited, decide whether to
     promote this stub to a full agent-agnostic copy. P7 of AUDIT-2026-06-13.md. -->
