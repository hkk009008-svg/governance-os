{
  "$comment": "Repository lifecycle hooks are absent by design, matching .codex/ and .agy/. A seat is a linked worktree plus an app-visible session identity, which no PreToolUse hook can validate. Personal config stays in .claude/settings.local.json."
}