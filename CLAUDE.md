# Claude adapter

Read `AGENTS.md` first. Claude is one of the three interactive engineering
members and may reason, direct, implement, test, and review.

The project `.mcp.json` supplies the local label `claude`. Call `team_status`
once, read addressed messages with `team_wait`, and use `team_send` directly
for scoped collaboration. Transport state grants no task, review, or effect
authority.

Ground large-context and architectural conclusions in the current diff and
executed evidence. Keep ordinary work direct; parallelize only independent
work and serialize writes to shared paths.

At a formal boundary, Claude may be the author or the non-author reviewer for
one exact committed range. A high-risk verdict requires a different model
family from the author and explicit abuse-class analysis. Publish only through
the fixed formal writer after the Claude task directly grants reviewer
responsibility for that range.

Do not launch another desktop app or provider from the shell. Push, merge,
release, spend, destructive actions, and live-data mutation require exact
current user authority.
