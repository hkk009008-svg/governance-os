---
description: Cursor app-seat identity, worktree boundaries, and mailbox policy
alwaysApply: true
---

# Cursor app seats

Cursor Desktop/Agents Window is the normal runtime. The main checkout and
ordinary branches are a **readiness bridge**: reads and `.pytest-verify-tmp/`
scratch are free; every governed mutation or effect surfaces one in-app
approval.

A live seat is one fact: a linked worktree on reserved branch
`cursor-seat/<seat>` whose user-local registry record (root, active top-level
app `conversation_id`, app-visible selected model ID) was written by the
`sessionStart` hook. Wrappers and hooks resolve that identity at point of use;
environment variables describe it but never grant authority. Cursor app seats
use their worktree's native Git index and reject `GIT_INDEX_FILE`.

- `director`/`director2` implement without per-action approval in their own
  worktree.
- `operator`/`operator2` review and issue GO/NITS/FAIL; their one unattended
  commit is the exact event path just staged by the fixed writer.
- `coordinator` reconciles, authors no production changes, and holds no
  mailbox cursor.
- The standing pair is `director` plus `operator2`; other seats are on-demand
  capacity lanes.
- Behavior-changing acceptance requires a non-author Operator using a different
  selected model ID against one committed verify-request. This is app-visible
  evidence, not provider/backend attestation.

Mailbox publish/consume uses `cursor-publish`/`cursor-consume`, delegates to
the fixed writers, and requires an in-app approval. Never edit mailbox, lock,
or `.cursor/runtime/` state directly — the hook hard-denies that. Push, pull,
fetch, merge, rebase, cursor consume, and paid spend are separate effects;
each needs its own in-app approval for the exact action.

Never switch branches inside a seat chat on a dirty tree; the app may
auto-commit a checkpoint. Seats stay on their reserved branch.

Custom subagents are optional advisors or capacity workers. They are not
durable seats, cannot inherit seat authority, and cannot publish verdicts.
Full seats are pinned top-level Agents Window chats in dedicated worktrees.
